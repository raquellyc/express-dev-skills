<%- include('../partials/header') %>

<h1>Skills</h1>
<ul>
  <% skills.forEach(function(s) { %>
    <li>
      <a href="/skills/<%= s.id %>">
        <%= s.todo %>
      </a>
        - 
      <%= s.done ? 'done' : 'not done' %>
    </li>
  <% }); %>
</ul>

<%- include('../partials/footer') %>